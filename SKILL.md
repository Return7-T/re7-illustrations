---
name: re7-illustrations
description: 使用固定的 Re7 个人 IP 与已确认的视觉风格，为文章、博客、帖子、知识内容、产品方法论和 AI 工作流规划或生成贴合原文、带少量中文批注的正文配图。用于用户要求生成文章配图、插画、shot list、视觉隐喻、概念图、中文标注、改图或检查 IP/风格/内容一致性时；支持从原文提取认知锚点、逐张生成、QA 与返工。
---

# Re7 Illustrations

用固定 IP“Re7”把文章中的具体判断变成角色亲自参与、读者一眼能懂的视觉隐喻。画面必须贴合原文，并默认使用少量有解释作用的中文批注。

## 每次先读

开始任何配图任务前读取：

- `references/ip-dna.md`：角色身份、动作与不可改变项。
- `references/style-dna.md`：整张图的视觉语言。
- `references/content-mapping.md`：从原文到认知锚点、对象与动作的映射。
- `references/annotation-system.md`：中文批注的选词、位置与生图规则。
- `references/pose-and-anatomy.md`：动作容量、左右手计划、参考图选择与肢体硬性验收。

按需读取：

- 规划视觉结构：`references/composition-patterns.md`。
- 生成图片或完整提示词：`references/prompt-template.md`。
- 检查或返工：`references/qa-checklist.md`。
- IP 一致性不稳时：查看 `assets/ip-reference*`；多图时查看 `assets/ip-reference/`。若文件中有多个姿态，只用于挑选并裁出一个单姿态参考，不把整张设定图直接传给场景生图。

## 工作流

### 1. 建立内容地图

先按 `references/content-mapping.md` 阅读全文，不要看见主题就直接画。为候选段落提取：

- 原句锚点或准确段落位置。
- 作者真正的判断，而不只是话题名。
- 关系类型：因果、对比、转化、顺序、分层、反馈、瓶颈或状态变化。
- 原文中的关键名词、动词和结果词。
- 读者看完图应记住的一句话。

不要平均配图。默认选择 4-6 个高价值认知锚点；短文 1-3 个，长文通常不超过 9 个。

### 2. 生成 shot list

每张图必须写清楚：

- 放置位置与原句锚点。
- 读者 takeaway。
- 结构类型和文章关系。
- 针对本段的新视觉隐喻。
- Re7 承担的不可替代动作。
- 姿态计划：角色数量、身体朝向、左臂/左手、右臂/右手、遮挡与可见手数量。
- 原文概念 → 画面对象的逐项对应。
- 中文文字计划：逐字文本、作用、绑定对象、位置和颜色。
- 画幅与主要留白区域。

默认规划 2-6 个短中文批注，而不是只放一个巨大标题；仅当用户明确要求无字时省略。连续图片至少使用两种结构或角色动作，避免反复使用机器、桥、漏斗或左右流程。

用户只要求规划时，到 shot list 为止。用户明确要求生成时，不等待额外确认，继续逐张生成。

### 3. 逐张生成

先根据 `references/prompt-template.md` 合并内容地图、文字计划、IP DNA 与 Style DNA，再调用图像工具。每张图只表达一个核心判断。

先按 `references/pose-and-anatomy.md` 验证动作可行性。默认一个角色实例、一个主要动作、最多两只可见手；三个以上独立动作必须删减、拆图或改由设备输出和结果对象表达。

有图像生成工具时：

- 每张单独调用，不把多张拼成一张。
- 把单姿态角色图作为图像参考；只选最接近当前姿态的一张。若现有资产是多姿态设定图，先裁出一个完整单姿态角色再附加，禁止直接使用整张设定图生场景。
- 在 prompt 中逐字引用每条中文，并明确绑定对象与位置；禁止模型添加未列出的文字。
- 保留原始生成结果；迭代时新建文件，不覆盖通过版本。

没有图像生成工具时，输出每张图可直接使用的完整 prompt，并明确说明尚未完成像素级 QA。

### 4. QA 与返工

逐张执行 `references/qa-checklist.md`。除了角色与画风，还必须检查：

- 角色数量和四肢是否符合姿态计划；每只可见手能否连续追溯到肩膀。
- 是否有额外手臂、额外手、悬空断臂、融合肢体或从桌面/物体/躯干冒出的手。
- 画面是否表达了原文的具体判断，而不只是泛化主题。
- 至少两个关键对象和一个动作能对应回原文。
- 中文批注是否有解释作用、逐字正确并贴近对象或关系。
- 是否出现海报式大标题、随机文字或 PPT 式标签框。

肢体错误直接判失败，优先简化为一个主要动作并整张重生成；不要用文字计划或风格调整掩盖。其他问题再局部定位：身份漂移改 IP DNA；全局画风漂移改 Style DNA；内容泛化改内容映射；角色装饰化改构图；文字失败改文字计划或局部编辑。修改全局 DNA 后抽查一张已通过图片，防止回归。

### 5. 保存交付

在 workspace 中保存到：

```text
assets/<article-slug>-illustrations/
```

按顺序命名 `01-topic-name.png`、`02-topic-name.png`。交付时说明图片数量、对应段落、路径和仍需人工确认的项。

## 三个关键判断

- 如果去掉 Re7 后隐喻仍完整成立，角色只是装饰，必须改动作。
- 如果换一篇同主题文章仍能原样使用这张图，内容映射太泛，必须回到原句锚点。
- 如果删掉所有批注仍完全不影响理解，检查文字是否只是装饰；保留真正帮助读者定位对象、关系或转折的词。
- 如果任一可见手无法沿手腕、手臂连续追溯到肩膀，或一个角色超过两只手，图片必须返工，不能交付。
